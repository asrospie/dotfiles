# Dotfiles

My personal dotfiles and development environment configuration.

This repository is where I keep the configuration files, shell customizations, and tooling preferences I use across my machines.

## Usage

Clone the repository and review its contents before linking or copying any files into your home directory:

```sh
git clone <repository-url> ~/.dotfiles
```

Configuration and setup instructions will live alongside the files they support.
